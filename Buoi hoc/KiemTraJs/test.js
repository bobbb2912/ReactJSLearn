let a = 2
const fs = require('fs');
const readlineSync = require('readline-sync'); 
transcript = readlineSync.question('Transcript? ');
student = {name: 'hien', age: '21'};
number = parseFloat(transcript)
console.log(student.toString);